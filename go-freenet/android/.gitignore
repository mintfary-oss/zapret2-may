*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/

# Generated gomobile AAR — built locally via scripts/build-android.sh.
# Not tracked in git; each developer builds it from source.
app/libs/mobile.aar
app/libs/mobile-sources.jar

# Release keystore — decoded by CI from KEYSTORE_BASE64 secret, never committed.
app/signing.jks
